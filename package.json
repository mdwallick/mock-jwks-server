{
    "name": "mock-jwks-server",
    "version": "1.0.0",
    "description": "A mock server for JWKS URI endpoints",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "deploy": "gcloud run deploy mock-jwks --source . --allow-unauthenticated"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^5.1.0"
    }
}